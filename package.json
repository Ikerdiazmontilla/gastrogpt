
{
  "name": "gastrogpts-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:dev": "concurrently \"npm start --prefix frontend\" \"npm run dev --prefix backend\"",
    "build:frontend": "npm run build --prefix frontend",
    "start:proxy-local": "npm run build:frontend && concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix web-server\"",
    "install-all": "npm install --prefix frontend && npm install --prefix backend && npm install --prefix web-server"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
